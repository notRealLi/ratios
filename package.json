{
  "name": "ratios",
  "version": "1.0.0",
  "description": "react project",
  "main": "server.js",
  "scripts": {
    "start": "npm run server && cd .. && npm run client",
    "server": "cd back && npm i && node server",
    "client": "cd front && npm i && npm start",
    "dev": "concurrently \"cd back && nodemon server\" \"cd front && npm start\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
